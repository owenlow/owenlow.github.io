{
  "name": "owenlow-github-io",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && cp CNAME out && gh-pages -d out -t true"
  },
  "dependencies": {
    "bootstrap": "^4.5.3",
    "classnames": "^2.2.6",
    "gray-matter": "^4.0.2",
    "next": "10.0.1",
    "raw-loader": "^4.0.2",
    "react": "17.0.1",
    "react-bootstrap": "^1.4.0",
    "react-dom": "17.0.1",
    "react-markdown": "^5.0.3"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
